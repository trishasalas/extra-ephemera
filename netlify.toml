[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"

# Enable Netlify Blobs
[dev]
  # Uncomment the next line to use production blobs during local dev
  # Note: requires `netlify link` and authenticated session
  blobsContext = "deploy"
